import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';
// 같은폴더에 있다 ./

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

